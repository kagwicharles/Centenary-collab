import 'package:flutter/material.dart';

class OnTheGOLanding extends StatelessWidget {
  const OnTheGOLanding({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("On The GO"),
      ),
    );
  }
}
